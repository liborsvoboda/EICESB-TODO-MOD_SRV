```csharp
@{ 
    var a = 1;
    var b = 2;
}
Basic Calc
a + b = @(a + b)
```

The equivalent in `#Script` using `code` language blocks:

```html
<script>
var a = 1
var b = 2
</script>
Basic Calc
a + b = {{a + b}}
```
